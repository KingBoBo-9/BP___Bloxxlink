Questions:

To do:
    - adjust spawn so things can't spawn on same tile 
    - make cargo collision at edge for player 
    - start en eindscherm 
    

N.B.
- Constanten!
- Comments!
- ARRAYS!
- GETTERS EN SETTERS!!! 
- vergeet niet om terug te zetten in processing
- getters en setters bij die cargo thing
- initCargo ramp
- moet minimaal 1 array in zitten 
- beoordeling SEB in brightspace 
- overlap spawn van cargo / obstacle 
- punt negen pull cargo note afmaken
- setup en draw zo klein mogelijk houden
-test modulen aanmaken Test_Robot of Test 
    - header van test heeft geen parameters 
    - testen voor methods met return values 
    - test modules komen niet terug in architectuur tekening
- Op te leveren: het opleverdocument, 12.1 in reader reflectie op code kwaliteit 
- code kwaliteit is beangrijk!!



REFACTOR:
- ALLES
- boolean names
- EndScreen code (is van huiswerk opdracht)
    - electric field 


De code maakt zelf zo veel mogelijk duidelijk wat het doet. 
Als je moet uitleggen wat een simpele regel doet, is je code waarschijnlijk niet duidelijk genoeg geschreven.



-- movePlayer 2.0
-- - calculate playerTarget
-- - check if playerTarget is within grid
-- - check if playerTarget is in obstacle range
-- - check if cargo on playerTarget
--     if cargo:
--         - calculate cargoTarget
--         - check if cargoTarget is within grid
--         - check if cargoTarget is in obstacle range
--         - check if cargo at cargoTarget
--         - move cargo
-- - move player
-- - update score
-- - check pullCargoMode?


-- movePlayer()
-- 1. richting omzetten naar directionCol / Row ✅
-- 2. player target berekenen ✅
-- 3. grid grenzen checken ✅
-- 4. check if cargo ✅
--     if cargo: ✅
--         5. bereken cargo target ✅
--         5.1 check of cargo op cargo target ✅
--         6.  cargo grid grenzen checken ✅
--         7. cargo verplaatsen ✅
-- 8. speler verplaatsen ✅
-- 9. check if pullCargoMode = true
--         if true:
            

//Determine which screen to show

